[package]
name = "iago-shared"
version = "0.1.0"
edition = "2021"
description = "Shared helpers for the GitHub issues resolver Rust workspace."

[lib]
name = "iago_shared"
path = "src/lib.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
